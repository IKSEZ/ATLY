$kind: http-request
name: 'Buscar atleta por ID'
method: GET
url: '{{baseUrl}}/api/v1/atletas/:id'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{token}}'
pathVariables:
  - key: id
    value: '{{atletaId}}'
